#!/bin/sh
set -eu

STAGED_JAVA_FILES=$(git diff --cached --name-only --diff-filter=ACMR | grep -E '^src/(main|test)/java/.*\.java$' || true)

echo "Running Spotify Java formatter..."
./mvnw -q fmt:format

if [ -n "$STAGED_JAVA_FILES" ]; then
  printf '%s\n' "$STAGED_JAVA_FILES" | xargs git add
fi
